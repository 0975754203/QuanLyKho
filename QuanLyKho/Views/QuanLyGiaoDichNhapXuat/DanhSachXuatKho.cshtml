@using QuanLyKho.Utility
@model QuanLyKho.Models.KhoGiaoDich_DSModel
@{
    var laCoQuyenThaoTac = Global.CoQuyenThaoTacDuLieuKho();
}

<style>
    .xuat-list-table th,
    .xuat-list-table td {
        text-align: center;
        vertical-align: middle !important;
    }

    .xuat-list-actions {
        display: flex;
        justify-content: center;
        gap: 8px;
        align-items: center;
    }

    .xuat-action-btn {
        width: 34px;
        height: 34px;
        border-radius: 6px;
        border: 1px solid #dbe4ee;
        background: #ffffff;
        color: #345;
        display: inline-flex;
        align-items: center;
        justify-content: center;
    }

    .xuat-action-btn:hover,
    .xuat-action-btn:focus {
        color: #ffffff;
        background: #1f7a63;
        border-color: #1f7a63;
    }

    .xuat-action-btn.delete:hover,
    .xuat-action-btn.delete:focus {
        background: #c0392b;
        border-color: #c0392b;
    }
</style>

<div class="table-responsive">
    <table class="table table-striped table-bordered verticle-middle table-hover table-responsive-fix-big xuat-list-table">
        <thead>
            <tr>
                <th scope="col">STT</th>
                <th scope="col">Mã giao dịch</th>
                <th scope="col">Thời gian</th>
                <th scope="col">Kho</th>
                <th scope="col">Khoa/Phòng</th>
                <th scope="col">Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @{ var i = 0; }
            @if (Model.TBL_KhoGiaoDich != null && Model.TBL_KhoGiaoDich.Count > 0)
            {
                foreach (var item in Model.TBL_KhoGiaoDich)
                {
                    <tr>
                        <td>@(++i)</td>
                        <td>@item.MaGiaoDich</td>
                        <td>@item.NgayTao.ToString("dd/MM/yyyy HH:mm")</td>
                        <td>@item.sTenKho</td>
                        <td>@(string.IsNullOrWhiteSpace(item.sTenKhoaPhong) ? "—" : item.sTenKhoaPhong)</td>
                        <td>
                            <div class="xuat-list-actions">
                                <a href="@Url.Action("InXuatKho", "QuanLyGiaoDichNhapXuat", new { id = item.Id })" target="_blank" class="xuat-action-btn" title="In"><i class="ti-printer"></i></a>
                                @if (laCoQuyenThaoTac)
                                {
                                    <button type="button" onclick="DeleteXuatKho('@item.Id')" class="xuat-action-btn delete" title="Xóa"><i class="ti-trash"></i></button>
                                }
                            </div>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="6">Không có dữ liệu</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@Html.GenPaging().PagingHTML(Model.PageIndex, Model.Pagecount, Model.Total)
<input type="hidden" id="pageIndex" name="pageIndex" value="@Model.PageIndex" />
