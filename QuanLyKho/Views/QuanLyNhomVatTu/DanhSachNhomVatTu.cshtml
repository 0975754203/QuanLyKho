@using QuanLyKho.Utility
@model QuanLyKho.Models.NhomVatTu_DSModel
@{
    var coQuyenThaoTac = Global.CoQuyenThaoTacDuLieuKho();
    var danhSach = Model.TBL_NhomVatTu ?? new System.Collections.Generic.List<BusinessLogic.Model.NhomVatTuModel>();
    var stt = (Model.PageIndex - 1) * Model.PageSize;
}

<div class="table-responsive">
    <table class="nhom-list-table">
        <thead>
            <tr>
                <th style="width:70px;">STT</th>
                <th style="width:160px;">Mã nhóm</th>
                <th>Tên nhóm</th>
                <th style="width:110px;">Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @if (danhSach.Count > 0)
            {
                foreach (var item in danhSach)
                {
                    <tr>
                        <td>@(++stt)</td>
                        <td>@item.MaNhom</td>
                        <td class="text-left">@item.TenNhom</td>
                        <td class="nhom-action-cell">
                            @if (coQuyenThaoTac)
                            {
                                <a href="javascript:void(0)" onclick="FormNhomVatTu('@item.Id')" class="nhom-action-btn" title="Sửa"><i class="ti-pencil-alt"></i></a>
                                <a href="javascript:void(0)" onclick="DeleteNhomVatTu('@item.Id')" class="nhom-action-btn danger" title="Xóa"><i class="ti-trash"></i></a>
                            }
                            else
                            {
                                <span class="text-muted">—</span>
                            }
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4">Không có dữ liệu</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@Html.GenPaging().PagingHTML(Model.PageIndex, Model.Pagecount, Model.Total)
<input type="hidden" id="pageIndex" name="pageIndex" value="@Model.PageIndex" />
